<h1><%= t ".title" %></h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: Settings.avar_comment.size %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
<%= paginate @users, theme: "twitter-bootstrap-3" %>
